{
  "name": "xpense-bend",
  "version": "1.0.0",
  "description": "server storage for my software application",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    // "serve-account": "json-server -p 3500 --w data/account.json",
    // "serve-notes": "json-server -p 3500 --w data/notes.json",
    // "serve-budget": "json-server -p 3500 --w data/budget.json",
    // "serve-earnings": "json-server -p 3500 --w data/earnings.json",
    // "serve-expenses": "json-server -p 3500 --w data/expense.json",
    // "serve-savings": "json-server -p 3500 --w data/savings.json",
    // "serve-balance": "json-server -p 3500 --w data/balance.json",
  },
  "author": "okolipraiz",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
