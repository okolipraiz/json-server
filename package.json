{
  "name": "xpense-bend",
  "version": "1.0.0",
  "description": "server storage for my software application",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "serve-json": "json-server --watch ./data/notes.json --port 3500 --routes routes.json"
  },
  "author": "okolipraiz",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
